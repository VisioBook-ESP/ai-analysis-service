name: ai-analysis
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    command: ["uvicorn", "src.api.app:app", "--host", "0.0.0.0", "--port", "8083"]
    ports:
      - "${APP_PORT:-8083}:8083"
    restart: unless-stopped
